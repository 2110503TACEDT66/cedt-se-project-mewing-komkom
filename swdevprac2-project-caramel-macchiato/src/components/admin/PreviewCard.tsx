import React from "react";

export default function PreviewCard({
  img,
  name,
  desc,
  seat,
}: {
  img: string;
  name: string;
  desc: string;
  seat: string;
}) {
  return <div>PreviewCard</div>;
}
